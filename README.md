Implemented a C++ console program that calculates a customer’s invoice based on item quantity, price, discount tiers, and membership status.
Includes subtotal calculation, discount logic, membership discount, and final total.
Formatted output using setw, setfill, fixed, showpoint, and setprecision(2).
Added comments for clarity and readability.
Created a C++ console program that evaluates college admission eligibility based on GPA, SAT score, and extracurricular participation.
Program determines acceptance using the specified criteria and displays a fully formatted results summary.
Used setw, setfill, fixed, and setprecision(2) for clean alignment.
Included descriptive comments for maintainability.
Developed a C++ console application that calculates a student's final average from three assignments and determines the appropriate letter grade based on a full +/- grading scale.
Output is formatted using setw, setfill, fixed, showpoint, and setprecision(2) for clear presentation.
Added structured comments to explain logic and improve future code maintenance.
